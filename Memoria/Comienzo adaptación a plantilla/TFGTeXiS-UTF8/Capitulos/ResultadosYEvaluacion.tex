\chapter{Resultados y Evaluación}
\label{cap:ResultadosYEvaluacion}

En este capítulo se muestran las pruebas realizadas sobre el modelo. El objetivo es el de evaluación del rendimiento del sistema y la experimentación en el uso de las
diferentes combinaciones de espectrogramas, arquitecturas y funciones de pérdida. Para ello, la fase se divide en dos vertientes:
evaluación sobre \textit{Test Set} y evaluación sobre \textit{Benchmark}. 

En la primera, se utilizan las muestras reservadas en la división inicial del dataset (un 15\%).
Los índices exactos de dicha muestra, se consiguen del archivo de punto de control (\textit{checkpoint}), evitando datos cruzados 
con los vistos en el entrenamiento. 

En cuanto a la evaluación sobre \textit{Benchmark}, su objetivo 
es poner a prueba el manejo del modelo dada la característica inyectividad nula de la síntesis FM. Para
ello, se diseña un banco de pruebas (\textit{benchmark}) con 12 sonidos de referencia que abarcan todo el espacio tímbrico
del sintetizador (desde un bajo limpio a sonidos agudos, complejos y muy ruidosos).

Sobre los resultados obtenidos por las dos vertientes previas, se pueden calcular diferentes métricas
para la evaluación del modelo en ambos ámbitos:

\begin{itemize}

\item \textbf{Métricas Sobre Parámetros: } Estas evalúan el modelo en función de la precisión obtenida
en los parámetros inferidos. Se emplean las métricas de: Error Cuadrático Medio (MSE), que penaliza las desviaciones paramétricas grandes 
entre predicción y original; Raíz del Error Cuadrático Medio (RMSE), que devuelve el error en las unidades originales de cada parámetro, consiguiendo
ver de forma directa la precisión y así poder hacer comparaciones objetivas entre las distintas arquitecturas y funciones de pérdida;
y Error Absoluto Medio (MAE), el cual es más robusto frente a valores atípicos. Estas métricas sufren ante el problema de la inyectividad 
nula en la síntesis FM, por lo que se utilizan principalmente como diagnóstico rápido.

\item \textbf{Métricas de Audio: } Estas son mucho más interesantes para conseguir una evaluación
similar a la que daría un oído experto, ya que vuelven a sintetizar los parámetros y realizan una comparativa
perceptual de los sonidos. Primero se aplica la Distancia L1 en Espectrogramas Mel (\textit{Mel l1}), la cual calcula el 
Error Absoluto Medio (L1) sobre las señales previamente transformadas a la escala Mel, consiguiendo así la sensibilidad logarítmica
del oído humano. De igual manera se aplica la Distorsión Cepstral Mel (MCD - \textit{Mel-Cepstral Distortion}). Es una métrica que trata 
de evaluar la similitud del timbre de los audios, extrayendo 13 Coeficientes Cepstrales en las Frecuencias de Mel (MFCC). 
El primer coeficiente, que representa la energía global, es descartado para que la métrica refleje exclusivamente el timbre (textura del sonido).

\item \textbf{Métrica de Reconstrucción (Decoder L1): } En el caso en el que se utilice la arquitectura
\textbf{CNNRegressor5}. Calcula el Error Absoluto Medio (L1) entre el espectrograma original y el reconstruido por la red,
de forma que se consigue evaluar la asimilación de información en el espacio latente. 

\end{itemize} 

\section{Definición de los Pipelines Experimentales}

\begin{figure}[H] 
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/pipelines}
    \caption{Representación de las diferentes combinaciones de Arquitecturas y Funciones de Pérdida.}
    \label{fig:pipelines}
\end{figure}

El diagrama \ref{fig:pipelines} muestra las diferentes topologías que se dan combinando las arquitecturas y funciones de pérdida previamente definidas.

\section{Características del \textit{Hardware} empleado en el entrenamiento de los modelos}

Para la contextualización de los datos de tiempos requeridos en el entrenamiento de los diferente modelos, se especifican las características de las máquinas empleadas:
\noindent \textbf{\textit{GPUs}:}
\begin{itemize}
    \item \textbf{Máquina Cedida por la Universidad: }NVIDIA GeForce RTX 3070
    \item \textbf{Ordenador Personal David: }NVIDIA GeForce RTX 3050 
\end{itemize}

%En cada apartado se especifica la máquina utilizada para su entrenamiento.

%(TODO) Insertar tabla con las características

\section{Evaluación de las Diferentes Arquitecturas y Funciones de Pérdida}
La evaluación de las topologías se ha llevado a cabo con un único dataset de 50000 audios (método de generación explicado aquí: \ref{sec:GeneracionDataset}), un número que ha resultado ser equilibrado, perimitiendo unos resultados decentes sin suponer un incremento demasiado grande en los tiempos de entrenamiento de los modelos. 

El número de épocas no se ha mantenido fijo, se ha utilizado un valor de paciencia (\textit{Early Stopping}) de 10 épocas. De esta manera, se comprueba el error de validación en cada época y si despues de 10 seguidas no se ha mejorado su record histórico, la red deja de entrenarse. Así pues, se evita la Sobreadaptación (\textit{Overfitting}) de la red neuronal, puesto que se evita un entrenamiento excesivo, y se ahorra tiempo. 

Por otro lado, se ha elegido un \textit{Batch Size} de tamaño 32. Este, aparte de ser un estándar, es un valor asumible por cualquier tarjeta gráfica moderna y que consigue una eficiencia adecuada. 

\noindent \textbf{\textit{Benchmark}:}
\begin{itemize}
    \item \textbf{Tamaño del Dataset: }50000
    \item \textbf{Valor de Paciencia: }10 
    \item \textbf{\textit{Batch Size}: }32
\end{itemize}

\subsection{P1: CNNRegressorSimple: SmoothL1, STFT lineal}
P1 es un modelo con una arquitectura simple (\textit{CNNRegressorSimple}), una función de pérdida \textit{SmoothL1} y que gestiona espectrogramas de tipo \textit{STFT} lineales. El número de épocas que ha requerido para cumplir con el valor de paciencia ha sido de 35, obteniendo un error de validación final de 0.015953. Este valor indica que la red está consiguiendo predecir los parámetros con gran precisión. Sin embargo, debemos de tener en cuenta que en la síntesis FM, una pequeña desviación en un parámetro como indice de modulación, podría ocasionar la producción de un sonido completamente diferente al original. Además, cabe destacar que mediante la función de pérdida \textit{SmoothL1}, se está ignorando la inyectividad nula que caracteriza a FM. 

\subsubsection{Evaluación Mediante \textit{Test Set}}


\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/test_set_p1}
    \caption{Graficas que muestran los resultados de evaluar el conjunto de tests sobre el modelo P1 con las métricas Mel L1 y MCD.}
    \label{fig:test_set_p1}
\end{figure}

Las gráficas de los resultados de evaluar el modelo mediante el conjunto de prueba (\ref{fig:test_set_p1}) muestran una desviación estándar elevada en relación con la media para ambas métricas, lo que indica unos resultados muy dispersos. 

En el caso de \textit{Mel L1}, el resultado anterior se debe a que hay una gran cantidad de valores a ambos lados de la media a pesar de que no se alejen demasiado de esta. 

Con \textit{MCD} se puede apreciar que una gran parte de los valores se encuentran a la izquierda de la media (valores mejores que la media), sin embargo hay unos pocos que se alejan de manera exagerada de la media por la derecha, lo que aumenta la desviación estándar y empeora la media. 

Así pues, se llega a la conclusión que hay ciertos audios que la red gestiona peor que otros, por lo que se pasa a evaluar mediante el \textit{benchmark} de audios variados para analizar este comportamiento. 

\subsubsection{Evaluación Mediante \textit{Benchmark} Diverso}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/benchmark_P1}
    \caption{Gráfico obtenido en la evaluación de P1 mediante \textit{benchmark}.}
    \label{fig:benchmarkP1}
\end{figure}

Uno de los audios que ha obtenido mejores resultados en las métricas ha sido \textit{muy\_agudo}, mientras que \textit{indice\_maximo} ha sido el peor valorado. Analizando estos extremos se espera encontrar las razones detrás de la dispersión de los resultados sobre el conjunto de prueba.

El audio \textit{muy\_agudo}, se caracteriza por tener muy pocos armónicos y que estos estén bastante separados entre sí y en frecuencias muy altas.
Al analizar su espectrograma original frente al de la predicción del modelo (figura: \ref{fig:esp_muy_agudo_P1}), se aprecia que la red ha conseguido recrear los armónicos, aunque ha metido algunos de más. La forma de la envolvente también la ha gestionado bien, sin embargo no la ha conseguido colocar en el tiempo con demasiada exactitud. Al oído se puede apreciar la similitud entre original y predicción, siendo la segunda más estridente y con menos sonoridad. 

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/esp_muy_agudo_P1}
    \caption{Espectrograma resultado de la predicción de P1 del audio de Benchmark: \textit{muy\_agudo}.}
    \label{fig:esp_muy_agudo_P1}
\end{figure}

En cuanto a \textit{indice\_maximo}, se trata de un audio con un número de armónicos más elevado que el anterior. Además, tiene armónicos en frecuencias bajas con muy poca energía. Si observamos lo que ha hecho la red (\ref{fig:esp_indice_maximo_P1}), se ve claramente que sigue identificando más armónicos de los que debería y a los que están en frecuencias bajas les da una cantidad de energía desproporcionada. A pesar de esto, se ha desenvuelto positivamente con la envolvente de amplitud.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/esp_indice_maximo_P1}
    \caption{Espectrograma resultado de la predicción de P1 del audio de Benchmark: \textit{indice\_maximo}.}
    \label{fig:esp_indice_maximo_P1}
\end{figure}

Con este análisis, se puede concluir que los audios más complejos (con más armónicos y un timbre más elaborado), el modelo los predice peor que los más simples, justificando así su elevada dispersión. 

\subsection{P2: CNNRegressorSimple: SmoothL1, Mel (log-perceptual)}
El modelo con estructura de \textit{CNNRegressorSimple}, con función de pérdida \textit{SmoothL1} (explicada en el apartado \ref{sec:FuncionPerdida}) y espectrogramas Mel ha necesitado 57 minutos y 57 épocas para entrenarse, acabando con un error de validación 0.006844. Este resultado indica una desviación en la predicción de parámetros mínima. A pesar de esto, se da la misma situación que en el modelo P1, ya que se sigue sin tener en cuenta la inyectividad nula de la síntesis FM.  

\subsubsection{Evaluación Mediante \textit{Test Set}}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/test_set_p2}
    \caption{Graficas que muestran los resultados de evaluar el conjunto de tests sobre el modelo P2 con las métricas Mel L1 y MCD.}
    \label{fig:test_set_p2}
\end{figure}

%(TODO) Hablar de la varianza y desviación estándar y así explicar la consistencia del modelo, lo cual puede ser interensante para compararlos también

En la gráfica \ref{fig:test_set_p2}, se muestran los resultados de la evaluación del modelo con las métricas Mel L1 y MCD, sobre el conjunto de test (\textit{Test Set}) del dataset. Al no tener todavía la referencia de los resultados de los otros modelos, se hace un análisis inicial de la dispersión de los valores obtenidos en las métricas para así evaluar la consistencia del modelo. 

Al analizar los resultados obtenidos con \textit{Mel L1}, se pueden observar los siguientes datos: la media es de 0.063 y la \textbf{desviación estándar} es de 0.037. Esto indica una dispersión estadística alta. Lo mismo pasa al interpretar los resultados del \textit{MCD}: la media es de 244.662 y la desviación estándar es de 154.292, un número grande en proporción con la media. 

Estos resultados pueden indicar una consistencia pobre en el modelo, sin embargo, en la gráfica (\ref{fig:test_set_p2}) se visualiza perfectamente la razón de los valores obtenidos. En el caso de \textit{MCD}, el grueso de los valores no se aleja demasiado de la media, pero existen unos pocos valores muy distantes que aumentan la desviación estándar en gran medida.

El caso de \textit{Mel L1} difiere del anterior debido a que la distribución es más uniforme, pero se compensa consiguiendo mejores errores máximos. 

Este comportamiento nos indica que el modelo probablemente gestione mejor unos tipos de audios que otros. Para ello, se pasa a la fase de la evaluación mediante \textit{benchmark}, poniendo a prueba sonidos muy dispares. 

\subsubsection{Evaluación Mediante \textit{Benchmark} Diverso}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/benchmark_P2}
    \caption{Gráfico obtenido en la evaluación de P2 mediante \textit{benchmark}.}
    \label{fig:benchmarkP2}
\end{figure}



Al aplicar \textit{Mel L1} y \textit{MCD} sobre los resultados del \textit{benchmark}, obtenemos el gráfico \ref{fig:benchmarkP2}. A simple vista se aprecia que hay dos casos en los que las métricas, previamente mencionadas, se encuentran muy por debajo de la media. Estos casos son con el sonido \textit{bajo\_limpio} y con \textit{muy\_bajo}. Este hecho se debe a que las frecuencias bajas y limpias poseen patrones visuales muy claros y fáciles de asimilar para la red. Por ejemplo, si se observa el espectrograma de \textit{muy\_bajo} y el de la predicción de la red ante el mismo \ref{fig:esp_muy_bajo_P2}, se puede apreciar la claridad y similitud entre el original y la predicción.  

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/espectrogramas_muy_bajo_P2}
    \caption{Espectrograma resultado de la predicción del audio de Benchmark: \textit{muy\_bajo}.}
    \label{fig:esp_muy_bajo_P2}
\end{figure}

En contraposición, se hayan los resultados sobre el audio \textit{pad\_lento}, que destaca por haber generado un incremento de ambas métricas. Un \textit{pad} es un sonido de sintetizador caracterizado por comenzar muy lento (ataque bajo) y mantenerse. Comparando el espectrograma original de \textit{pad\_lento} y el de la predicción podemos sacar algunas conclusiones de estos resultados. Como se ve en la figura \ref{fig:esp_pad_lento_P2}, la red ha conseguido inferir perfectamente los parámetros relacionados con la envolvente, pero ha fallado considerablemente en la búsqueda de las frecuencias y los parámetros que las afectan, de los que depende el timbre (frecuencia). Por otra parte, también se puede llegar a la conclusión lógica de que, al ser un sonido de gran duración, se puede acumular un mayor error en las métricas. Esto es debido a que, a pesar de que dichas métricas calculan la media, como a la red identifica con gran precisión los silencios, los audios que contengan una mayor parte en silencio van a conseguir mejores métricas.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/espectrograma_pad_lento_P2}
    \caption{Espectrograma resultado de la predicción del audio de Benchmark: \textit{pad\_lento}.}
    \label{fig:esp_pad_lento_P2}
\end{figure}

\noindent \textbf{Media Obtenida en las Métricas:}
\begin{itemize}
    \item \textbf{Mel L1 por sonido: }0.028
    \item \textbf{MCD por sonido: }214.520
\end{itemize}

\subsection{P3: CNNRegressor5: HybridLoss, STFT lineal}
El número de épocas de entrenamiento requeridas por el modelo P3 ha sido de 59, después del cual ha sobrepasado el valor de paciencia acabando así su entrenamiento. El error de validación final ha sido, aproximadamente, de 0.383, un valor que parece reducido. Como la función de pérdida híbrida da prioridad a la diferencia media (L1) entre el espectrograma original y el reconstruido (con un peso de 1.0), significa que se está calculando una diferencia media de menos de 0.383 decibelios por pixel, confirmando una gran similitud entre los espectrogramas.

\subsubsection{Evaluación Mediante \textit{Test Set}}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/test_set_p3}
    \caption{Graficas que muestran los resultados de evaluar el conjunto de tests sobre el modelo P3 con las métricas Mel L1 y MCD.}
    \label{fig:test_set_p3}
\end{figure}

A diferencia de otras arquitecturas, P3 no parece tener una dispersión tan elevada, estando la mayoría de los resultados de ambas métricas al rededor de la media.

Aun así combiene identificar qué audios son los que gestiona mejor y peor. Para ello se recurre al \textit{benchmark}.

\subsubsection{Evaluación Mediante \textit{Benchmark} Diverso}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/benchmark_P3}
    \caption{Gráfico obtenido en la evaluación de P3 mediante \textit{benchmark}.}
    \label{fig:benchmarkP3}
\end{figure}

Como se puede apreciar en la gráfica, la mayoría de audios han obtenido puntuaciones similares con las dos métricas. Hay dos casos que están bastante por debajo de la media y son los del sonido \textit{percusivo} y y \textit{muy\_agudo}, que, como hemos podido observar en las pruebas sobre otros modelos, son bastante simples y cortos y las diferentes arquitecturas los gestionan bien. En cuanto al peor resultado, destaca el de \textit{pad\_lento}, superando con creces la media.

Este último destaca por ser largo, por eso es normal que acumule más error que otros, lo que le convierte también en un buen ejemplo para visualizar como la red gestiona la predicción. 

El la figura \ref{fig:esp_pad_lento_P3}, se puede observar que el modelo ha fallado totalmente en la predicción de los armónicos, poniendo de más y colocando mal el resto. Esto nos muestra que la red no está siendo precisa, a excepción de las envolventes. 

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/esp_pad_lento_P3}
    \caption{Espectrograma resultado de la predicción del audio de Benchmark: \textit{pad\_lento}.}
    \label{fig:esp_pad_lento_P3}
\end{figure}


\subsection{P4: CNNRegressor5: HybridLoss, Mel (log-perceptual)}
La red neuronal con estructura \textit{CNNRegressor5}, función de pérdida híbrida (\textit{HybridLoss}) y espectrogramas Mel, ha requerido de un total de 27 épocas para entrenarse, consiguiendo reducir el error de validación a 0.672. Este valor no es tan reducido como el del modelo anterior, pero indica una diferencia media de menos de 0.672 decibelios por pixel, siendo notablemente bajo y hay que tener en cuenta que, en este caso, se hace uso de espectrogramas de Mel, que consiguen una representación similar a la forma de percibir los audios de los humanos, por lo que se puede esperar que sean muy similares acústicamente. 

\subsubsection{Evaluación Mediante \textit{Test Set}}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/test_set_p4}
    \caption{Graficas que muestran los resultados de evaluar el conjunto de tests sobre el modelo P4 con las métricas Mel L1 y MCD.}
    \label{fig:test_set_p4}
\end{figure}

Los resultados de la evaluación mediante el conjunto de prueba aparecen reflecados en la gráfica \ref{fig:test_set_p4}. Esta muestra unos valores de media y desviación estándar en ambas métricas que indican una muy baja consistencia. Visualmente, se puede observar una distribución de gran uniformidad en la métrica \textit{Mel L1} y también, en menor medida, en \textit{MCD}, lo que implica que la calidad de las predicciones varía mucho entre distintos tipos de audio. 

Para ello se analiza el comportamiento de la red ante el \textit{benchmark}.


\subsubsection{Evaluación Mediante \textit{Benchmark} Diverso}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/benchmark_P4}
    \caption{Gráfico obtenido en la evaluación de P4 mediante \textit{benchmark}.}
    \label{fig:bench_p4}
\end{figure}

Como muestra la gráfica \ref{fig:bench_p4}, hay ciertos audios con los que el modelo consigue métricas reducidas respecto a la media y otros que reciben valores muy por encima de la media.

El audio \textit{percusivo} consigue las mejores métricas, aun así si nos fijamos en el espectrograma original y en el de la predicción, se aprecia perfectamente que el modelo no ha conseguido replicar las envolventes, llegando incluso a cortar el audio antes de tiempo. Aparte, consigue representar bastante bien los armónicos pero asigna más energía de la original en las frecuencias más bajas. La razón de haber conseguido colocarse como el sonido con mejor resultado para el modelo en el benchmark probablemente esté muy ligado a ser un audio muy corto, lo que le da ventaja (como se explicó en P2).

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/espectrograma_percusivo_P4}
    \caption{Espectrograma resultado de la predicción del audio de Benchmark: \textit{percusivo}.}
    \label{fig:esp_percusivo_P4}
\end{figure}

Por esta última razón, tiene sentido analizar también el siguiente mejor audio, el cual es \textit{bajo\_limpio}. De este se pueden sacar conclusiones mucho más claras observando la imagen \ref{fig:bajo_limpio_P4}, donde se observa que el modelo consigue representar fielmente los armónicos pero sin demasiada resolución, lo que hace que suenen muy distintos al oído. Además, sigue fallando en las envolventes, a pesar de no ser radicales, y no ha parado el audio en el momento exacto.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/espectrograma_bajo_limpio_P4}
    \caption{Espectrograma resultado de la predicción del audio de Benchmark: \textit{bajo\_limpio}.}
    \label{fig:bajo_limpio_P4}
\end{figure}

El peor resultado lo ha obtenido \textit{bajo\_rico}, lo que es coherente con lo mencionado con anterioridad, ya que este audio destaca por sus envolventes muy definidas.  


\subsection{P5: CNNRegressor5: MultiScaleSpectralLoss, STFT lineal}
El número de épocas que ha requerido la red para su entrenamiento es de 35, con un error de validación final de 0.4297. Este error viene de la suma $L_{total} = L_{lin} + \alpha \cdot L_{log} + 0.05 \cdot L_{params}$, lo que indica que apenas se penaliza el fallo en los parámetros (peso de 0.05) y que el grueso del error viene del error en el espectrograma (tanto el linal como el logarítmico). De esta forma la red le da prioridad absoluta a la similitud visual y acústica del original y la predicción.

\subsubsection{Evaluación Mediante \textit{Test Set}} 

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/test_set_p5}
    \caption{Graficas que muestran los resultados de evaluar el conjunto de tests sobre el modelo P5 con las métricas Mel L1 y MCD.}
    \label{fig:test_set_p5}
\end{figure}

La gráfica \ref{fig:test_set_p5} muestra una dispersión menor que otros modelos, conteniendo la mayor parte de los datos cercanos a la media. Esto nos indica que da unos resultados, hasta cierto punto, similares independientemente del audio, lo cual es positivo. 

Se procede a comprobar esta hipótisis sobre los audios del \textit{benchmark}.

\subsubsection{Evaluación Mediante \textit{Benchmark} Diverso}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/benchmark_P5}
    \caption{Gráfico obtenido en la evaluación de P5 mediante \textit{benchmark}.}
    \label{fig:bench_p5}
\end{figure}

Las gráficas de \ref{fig:bench_p5} muestran una distribución considerablemente uniforme para ambas métricas. Los resultados que más destacan serían los de \textit{bajo\_limpio} y \textit{agudo\_ruidoso}, teniendo el primero un resultado desfavorable y el segundo favorable.

La predicción generada por el modelo para \textit{bajo\_limpio} es auditivamente muy distante, siendo el sonido original grave y la predicción aguda. Además, si nos fijamos en los espectrogramas (\ref{fig:bajo_limpio_P5}), se aprecia perfectamente que los armónicos definidos en la predicción son mucho más altos que los del original. 
\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/esp_bajo_limpio_P5}
    \caption{Espectrograma resultado de la predicción del audio de Benchmark: \textit{bajo\_limpio}.}
    \label{fig:bajo_limpio_P5}
\end{figure}

Para entender que es lo que hace mal el modelo, vamos a analizar también lo que hace mejor. 

Al intentar reproducir la predicción de \textit{agudo\_ruidoso}, no suena nada y en el espectrograma tampoco aparece nada, está vacío. La mejor puntación que le está dando a la red, se la está dando a un audio sin sonido. Esto se debe a que este audio en concreto estaba compuesto en su mayor medida por silencio, de ahí la similaridad que ha sacado la red entre predicción y original. 

Para asegurarnos de que la red no está sacando datos poco válidos, vamos a analizar el siguiente audio que mejor procesa: \textit{medio\_armonico}. 

Tanto el sonido como su espectrograma \ref{fig:medio_armonico_P5} difieren mucho del original.
\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/esp_bajo_limpio_P5}
    \caption{Espectrograma resultado de la predicción del audio de Benchmark: \textit{medio\_armonico}.}
    \label{fig:medio_armonico_P5}
\end{figure}

A partir de todos los datos mencionados, se puede llegar a la conclusión de que P5 no es un modelo funcional. 


\subsection{P6: CNNRegressor5: MultiScaleSpectralLoss, Mel (log-perceptual)}
En último lugar, encontramos el modelo P6, cuyo entrenamiento ha sido de 60 épocas, con un error de validación final de 0.6974 tras alcanzar el valor de paciencia. A pesar de parecer un resultado peor de P5, no se puede comparar directamente debido que P6 utiliza espectrogramas de tipo Mel, que (como ya se ha explicado anteriormente), gestionan la información del sonido de manera similar al oído humano. 

\subsubsection{Evaluación Mediante \textit{Test Set}}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/test_set_p6}
    \caption{Graficas que muestran los resultados de evaluar el conjunto de tests sobre el modelo P6 con las métricas Mel L1 y MCD.}
    \label{fig:test_set_p6}
\end{figure}

En este caso, nos volvemos a encontrar con un gráfico (\ref{fig:test_set_p6}) de que muestra gran dispersión, con una gran cantidad de valores alejados de la media.


\subsubsection{Evaluación Mediante \textit{Benchmark} Diverso}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/benchmark_P6}
    \caption{Gráfico obtenido en la evaluación de P6 mediante \textit{benchmark}.}
    \label{fig:bench_p6}
\end{figure}

El diagrama \ref{fig:bench_p6} indica unos valores en su mayoría homogéneos sobre el \textit{benchmark}, destacando los audios de \textit{muy\_agudo} y \textit{bajo\_rico}. 

En el primer caso, se da un error en las dos métricas muy bajo. Al escuchar el original y su predicción, se aprecia un timbre verdaderamente similar, aunque con una diferencia en el \textit{pitch} significativa. Al comparar sus espectrogramas (\ref{fig:muy_agudo_p6}) observamos perfectamente de donde viene la diferencia en el tono, la predicción aplica armónicos inexistentes en el original. 

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/muy_agudo_P6}
    \caption{Espectrograma resultado de la predicción del audio de Benchmark: \textit{muy\_agudo}.}
    \label{fig:muy_agudo_p6}
\end{figure}

En cuanto a \textit{bajo\_rico}, la diferencia de timbre es mayor y se puede observar que la red no ha, sabido a partir de cierto momento, mantener la energía original (\ref{fig:bajo_rico_p6}).

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Imagenes/Bitmap/bajo_rico_P6}
    \caption{Espectrograma resultado de la predicción del audio de Benchmark: \textit{bajo\_rico}.}
    \label{fig:bajo_rico_p6}
\end{figure}

\section{Comparación de los Resultados Obtenidos}

\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{ 
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{|c||c|c|c||c|c|c|}
    \hline
    \multirow{2}{*}{\textbf{Pipeline}} & \multicolumn{3}{c||}{\textbf{Métricas Paramétricas (RMSE) $\downarrow$}} & \multicolumn{3}{c|}{\textbf{Métricas Acústicas $\downarrow$}} \\ \cline{2-7} 
    & \textbf{Carrier (Hz)} & \textbf{Ratio} & \textbf{Index} & \textbf{Mel L1 (dB)} & \textbf{MCD} & \textbf{Decoder L1} \\ \hline\hline
    \textbf{P1} & X.XX & X.XX & X.XX & 0.071 & 333.255 & N/A \\ \hline
    \textbf{P2} & X.XX & X.XX & X.XX & X.XX & X.XX & N/A \\ \hline
    \textbf{P3} & X.XX & X.XX & X.XX & X.XX & X.XX & X.XX \\ \hline
    \textbf{P4} & X.XX & X.XX & X.XX & X.XX & X.XX & X.XX \\ \hline
    \textbf{P5} & X.XX & X.XX & X.XX & X.XX & X.XX & X.XX \\ \hline
    \textbf{P6} & X.XX & X.XX & X.XX & X.XX & X.XX & X.XX \\ \hline
    \end{tabular}
    }
    \caption{Comparativa de métricas paramétricas y acústicas sobre el Test Set para los 6 pipelines. Las flechas ($\downarrow$) indican que un valor menor representa un mejor rendimiento. El Decoder L1 no aplica (N/A) en las arquitecturas simples.}
    \label{tab:resultados_cuantitativos}
\end{table}


% (TODO) Meter comparaciones de los espectrogramas generados por cada uno y compararlos con el original. Justificar esto un poco con que no podemos poner audios en la memoria (obviamente)